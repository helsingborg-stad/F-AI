# OpenAI API can be used when creating embeddings for documents.
# The API can also be used for answering user questions.
#
OPENAI_API_KEY=your-openai-api-key

# The directory to save Chroma's data to.
#
DB_PATH=path

# Name of the database collection for planning permissions.
#
DB_PLANNING_PERMISSION_COLLECTION_NAME=planning-docs

# Path to documents to embed.
# Documents in this path will be embedded and saved to the database.
# The embeddings can be used for answering user questions.
#
DOCUMENTS_TO_EMBED=path

# Path to documents to answer questions on
EXCEL_INPUT_PATH=./data/planing_qa.xlsx
EXCEL_OUTPUT_PATH=./data/planing_qa_output.xlsx

# GPT4 Model Name
GPT_4_MODEL_NAME="gpt-4"
#GPT_4_MODEL_NAME="gpt-3.5-turbo-16k"

# Path to file with user credentials.
# Used to setup access control to Chainlit.
#
USER_CREDENTIALS_FILE=./data/user_credentials.json

# Chainlit Auth Secret.
# Can be generated by running CLI command `chainlit create-secret`.
#
CHAINLIT_AUTH_SECRET=your-chainlit-auth-secret

# Sentry Configuration
# The Sentry DSN (Data Source Name) pointing to your Sentry project.
#
SENTRY_DSN=your-sentry-dsn

# The minimum logging level at which logs should be captured for Sentry.
# Accepts standard logging levels like DEBUG, INFO, WARNING, ERROR, etc.
#
SENTRY_LOGGING_LEVEL=ERROR

# The minimum logging level at which logs should be sent as events to Sentry.
# Accepts standard logging levels like DEBUG, INFO, WARNING, ERROR, etc.
#
SENTRY_EVENT_LEVEL=ERROR

# The rate at which traces should be sampled and sent to Sentry.
# It should be between 0.0 and 1.0. Default is 0.0 if not set.
#
SENTRY_TRACE_SAMPLE_RATE=0.0
